---
version: "2"
ratings:
  paths:
  - "src/**.js"
plugins:
  duplication:
    enabled: false
    checks:
      Similar code:
        enabled: false
    config:
      languages:
      - javascript
  fixme:
    enabled: true
